오버로딩과 오버라이딩은 객체 지향 프로그래밍의 다형성을 구현하는 대표적인 방법이다.

<br>

### 오버로딩
- 하나의 클래스 안에서 같은 이름의 메소드를 여러 개 정의하는 것
- 메소드의 이름만 같을 뿐 매개 변수의 개수나 타입이 달라야 한다.

<br>

### 오버라이딩
- 부모 클래스로부터 상속받은 메소드를 자식 클래스에서 재정의하는 것
- 부모가 물려준 기능을 그대로 사용하지 않고, 자식의 상황에 맞게 내용을 바꾸어 사용하는 것
